spring:
  main:
    web-environment: true
    show-banner: true
  datasource:
    url: "jdbc:mysql://127.0.0.1:3306/model_service?autoReconnect=true"
    user: "aa_user"
    driverName: com.mysql.cj.jdbc.Driver 
    maximum-pool-size: 8
    minimum-idle: 0
    type: com.expedia.adaptivealerting.modelservice.RdsIamAuthDataSource
  jpa:
    openInView: true
    database: MYSQL
    show-sql: false
    generate-ddl: false
    hibernate:
      naming_strategy: org.hibernate.cfg.ImprovedNamingStrategy

endpoints:
  health:
    sensitive: false

management:
  context-path: "/admin"

server:
  port: 8080